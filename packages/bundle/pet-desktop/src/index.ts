/** Optional DSH desktop-pet bundle runtime marker. */

/** Stable Cordis plugin name. */
export const name = 'pet-desktop-bundle'

/** The bundle's behavior is entirely declared by its patch list. */
export function apply(): void {}
